# {{ ansible_managed }}

[INCLUDES]

after = sendmail-common.local

[Definition]

#actionstart = printf %%b "Hi,\n
#              The jail <name> has been started successfully.\n
#              Regards,\n
#              Fail2Ban" | /usr/bin/s-nail -r <sender> -s "[Fail2Ban] <name>: started on <fq-hostname>" <dest>
actionstart =
actionstop = 

[Init]

dest = {{ inventory.mail.recipient }}
sender = {{ inventory.mail.from }}
sendername = acoby Fail2ban
